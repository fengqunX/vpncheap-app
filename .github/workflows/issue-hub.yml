name: Issue hub

on:
  issues:
    types: [opened, reopened, transferred, closed, labeled, unlabeled, assigned, unassigned]

permissions:
  contents: read

jobs:
  sync:
    uses: fengqunX/vpncheap-app/.github/workflows/issue-hub-sync-reusable.yml@b14497cb78952882c59a8a050d46d8476857e867
    secrets:
      project_token: ${{ secrets.VPNCHEAP_PROJECT_TOKEN }}
